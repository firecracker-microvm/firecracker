[package]
name = "demo_seccomp"
version = "0.1.0"
authors = ["Amazon Firecracker team <firecracker-devel@amazon.com>"]
edition = "2018"

[dependencies]
libc = ">=0.2.39"

utils = { path = "../../../../src/utils" }
seccompiler =  ">=0.2.0"

[workspace]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

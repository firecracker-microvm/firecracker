[package]
name = "jailer"
version = "0.24.3"
authors = ["Amazon Firecracker team <firecracker-devel@amazon.com>"]
edition = "2018"
build = "../../build.rs"

[dependencies]
libc = ">=0.2.39"
regex = ">=1.0.0"

utils = { path = "../utils" }

[package]
name = "firecracker"
version = "0.4.0"
authors = ["Amazon firecracker team <firecracker-devel@amazon.com>"]

[dependencies]
clap = "=2.27.1"

api_server = { path = "api_server" }
vmm = { path = "vmm" }

[profile.release]
lto = true

[workspace]
